// Adjust the following values to match your needs
// -----------------------------------------------
#define   servername "myserver"  // Set your server's logical name here e.g. if 'myserver' then address is http://myserver.local/
IPAddress local_IP(192, 168, 1, 216); // Set your server's fixed IP address here
IPAddress gateway(192, 168, 4, 1);    // Set your network Gateway usually your Router base address
IPAddress subnet(255, 255, 255, 0);   // Set your network sub-network mask here
IPAddress dns(192,168,4,2);           // Set your network DNS usually your Router base address
const char ssid_1[]     = "AndroidAPD17A";
const char password_1[] = "123456789m;
